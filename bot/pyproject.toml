[project]
name = "reflectboard-bot"
version = "0.1.0"
description = "Telegram bot for ReflectBoard"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.0",
    "pydantic-settings>=2.0",
    "structlog>=24.0",
]
